# План устранения замечаний приёмки по ТЗ v2.0

Источники требований:

- [`ZIMA_FleetOps-отчёт_приёмки_демо-стенда_по_ТЗ_v2.0.md`](ZIMA_FleetOps-отчёт_приёмки_демо-стенда_по_ТЗ_v2.0.md) — акт приёмки: ACC-001…ACC-035, целевая переработка главных дашбордов (§10), приоритеты (§13). Далее «Отчёт».
- [`ZIMA_FleetOps-ТЗ_v2.0_на_доработку_демо-стенда.md`](ZIMA_FleetOps-ТЗ_v2.0_на_доработку_демо-стенда.md) — целевое состояние итерации.
- [`ZIMA_FleetOps-общий_сценарий_демонстрации_и_приёмки_демо-стенда_v1.1.md`](ZIMA_FleetOps-общий_сценарий_демонстрации_и_приёмки_демо-стенда_v1.1.md) — приёмочный маршрут (шаги 1–17).
- [`TZ_V2.0_REWORK_PLAN.md`](TZ_V2.0_REWORK_PLAN.md) — выполненная база DMO-01..09.

Статус-трекер пакетов ведётся в этом файле. Приёмка каждого пакета — по рубрике AGENTS.md (A/B/C/D, ниже B = отклонить) с доказательствами (файл:строка, вывод теста).

# 1. Контрольный контракт данных (Отчёт §5, §7 шаг 1)

Противоречие ТЗ v2.0 (33/6 столкновений/986 669 ₽) и фактической легенды устраняется фиксацией контракта, рекомендованного Отчётом. **Пересборка набора не требуется** — текущий генератор уже соответствует контракту:

| Параметр           | Значение контракта                                                                                                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Инциденты          | 33 = 32 подтверждённых классифицированных закрытых + 1 активный неклассифицированный (INC-2026-0033, живой сбрасываемый) с нулевым подтверждённым влиянием до прохождения сценария |
| Столкновения       | 5 подтверждённых закрытых случаев, 5 ч 10 мин, 265 417 ₽                                                                                                                           |
| Сумма потерь       | 986 667 ₽ (344 168 + 450 000 + 192 499), отклонение −2 ₽ от ТЗ задокументировано (целые минуты)                                                                                    |
| Прочие контрольные | 1075 мин влияния; технедоступность 49 ч 30 мин; плановые часы 6 240; техдоступность 99,21 %; операционная доступность мощности 99,71 %                                             |

Масштаб парка 72 робота / флагман 30 (ACC-024, P1) — отдельное решение с пересборкой всех контрольных сумм; в этой итерации не выполняется (заморозка DMO-01). Выполняется лёгкая часть: реальные семейства моделей (Ronavi H1500, HIK Robot AMR, Geek+ P/M-series) вместо текстов «синтетическая демо-легенда» — правка строк без изменения сумм.

# 2. Пакеты работ

Приоритет Отчёта §13: P0 = ACC-001…008, ACC-022, ACC-023, ACC-025, ACC-027, ACC-032, ACC-035; P1 = ACC-024 (масштаб — отложен), ACC-026, ACC-028–031, ACC-033, ACC-034, точечные ACC-009…021 по мере попадания в маршрут.

### PKG-A. Домен, гейты и детерминированность — P0 (ACC-001, ACC-002, ACC-004, ACC-006, ACC-029, ACC-032)

- A1 (ACC-001). Гейт «Вернуть робота в парк» в карточке инцидента и в сторе: возврат разрешён только при восстановлении процесса + финальной причине + завершённой сервисной работе с результатом + положительном контрольном запуске. Кнопка disabled с перечнем незавершённых шагов.
- A2 (ACC-006). Permission-гарды на уровне стора `useDemoData` для всех мутаций (действие без права → ошибка, UI кнопки скрывает). Новые права: `incidents.safety` (подтверждение безопасности), `incidents.recover` (возврат робота), наблюдение — `events.create`. Финансовые роли и операционный директор — строго read-only.
- A3 (ACC-002). Централизованные предикаты состояния/очередей инцидента (единая машина состояний) — один модуль, используемый карточкой, списком, «Мой объект», ТОиР. Запрещены локальные копии логики готовности к закрытию/очередям.
- A4 (ACC-004). Детерминированное демо-время живого сценария: действия INC-2026-0033 пишут сценарные метки (открытие 09:12 → назначение резерва → ввод +25 мин → возврат +8 ч 28 мин), а не `Date.now()`. Длительности и деньги воспроизводимы: 25 мин / 29 167 ₽.
- A5 (ACC-032/ACC-004). Убрать псевдо-живой дрейф KPI (`Math.random()` + `setInterval` на Обзоре) и статические фикции («Обновлено 09:15»). Отсутствующие данные не подменяются числами.
- A6 (ACC-029). Убрать отдельную кнопку «Обеспечить безопасность»: безопасность — чек-условие закрытия и запись в истории (подтверждается в шаге регламента при назначении координатора/резерва), не самостоятельное действие с правом у всех.
- Приёмка: e2e «ранний возврат заблокирован», «финансист не мутирует», «25 мин / 29 167 ₽ после живого прогона»; валидатор — 0 ошибок.

### PKG-B. Права, область данных, ТОиР-вход — P0 (ACC-005/019, ACC-007)

- B1 (ACC-007). Объектовая область на всех экранах: `ReportsPage` под `useTenantScope`; deep-link карточки (инцидент/робот/объект/зона) вне области роли → редирект со штатным объяснением.
- B2 (ACC-005/019). Домашний экран SERVICE_ENGINEER — очередь ТОиР «мои работы» (исполнитель = текущий пользователь), представления «ожидает/в работе/ожидает запчастей/контрольный запуск». Раздел «Сервис и ТОиР» — в навигацию сайдбара.
- Приёмка: e2e SITE_MANAGER не видит чужие объекты в Отчётах; SERVICE_ENGINEER попадает в свою очередь.

### PKG-C. Единые метрики — P0 (ACC-023, ACC-016)

- C1. Модуль `src/data/metrics.ts`: единые селекторы техдоступности, операционной доступности мощности, потерь, MTTR с фиксированной формулой и периодом набора (30 дней; плановый фонд = роботы × 8 ч × 30 дней = 6 240 робот-ч). Все страницы (Обзор, Аналитика, списки/карточки роботов и объектов, Отчёты) считают только через него. Период и источник видны рядом с числом.
- C2 (ACC-016). Сводка объекта: раздельно операционное влияние / техническая недоступность / денежные потери; формула потерь в подсказке. Совпадение с реестрами гарантировано единым селектором.
- Приёмка: e2e «одинаковые KPI на всех экранах» (робот в списке = робот в карточке; объект в списке = карточка = аналитика).

### PKG-D. Ролевые главные страницы — P0 (ACC-022, ACC-025, ACC-026, ACC-027)

- D1. Роутинг по роли после входа: SITE_MANAGER → «Мой объект» (есть); FLEET_OPERATIONS_MANAGER / SERVICE_MANAGER → новая «Портфель роботизации»; OPERATIONS_DIRECTOR + FINANCE_MANAGER → новая «Экономика эксплуатации»; SERVICE_ENGINEER → очередь «Мои работы» (B2); SYSTEM_ADMIN / SHIFT_OPERATOR → Обзор.
- D2. «Портфель роботизации» (Отчёт §10.4): вердикт портфеля, ключевые показатели (техдоступность, операционная доступность, MTTR, повторные, бэклог, потери), карточки объектов с сортировкой по критичности, системные причины (повторяемые на ≥2 объектах), очередь решений. Переходы «портфель → объект → зона → робот → инцидент» с сохранением периода.
- D3. «Экономика эксплуатации» (Отчёт §10.5): потери за период, Δ к предыдущему периоду, объекты с максимальными потерями, доля подтверждённых расчётов, очередь финансово-операционных решений; каждая сумма раскрывается до простоев/ставки/формулы. Только подтверждённые потери; TCO/окупаемость/эффект план-факт отсутствуют (нет базы — не подставляются). Роль строго читающая (A2).
- D4 (ACC-025). «Мой объект»: вердикт из состояния зон (не из случайных KPI), кликабельность всех показателей и блоков внимания в точный срез.
- Приёмка: e2e трёх ролевых главные; ручной прогон ролей по Отчёт §12.

### PKG-E. Идентификаторы и терминология — P1 (в объёме маршрутного шума) (ACC-008, ACC-020, ACC-030, ACC-031, ACC-010)

- E1 (ACC-008). Реестр событий: канонический INC-2026-XXXX вместо внутренних inc-NNN.
- E2 (ACC-031). Термин «интервал» из пользовательского текста → «простой с влиянием на процесс» / «техническая недоступность».
- E3 (ACC-030/024-лёгкая). Убрать «синтетическая демо-легенда», «класс решения», «модель не подтверждена» из UI; модели — реальные семейства (правка строк генератора без сумм).
- E4 (ACC-020). Паддинг идентификаторов в raw-сообщениях (FMR-001, не FMR-1); «p90» → «90-й перцентиль».
- E5 (ACC-010). Гарантированный ввод предварительной причины для активного инцидента (классификатор + обязательный человеческий комментарий).
- Приёмка: grep-аудит e2e/юнит по запрещённым строкам.

### PKG-F. Аналитика и drill-down — P1 (ACC-011, ACC-012, ACC-013, ACC-015)

- F1 (ACC-011). Детализация причины: шапка (роботы/объекты/зоны) считается из тех же строк, что таблица.
- F2 (ACC-013). Карточки потерь по объектам кликабельны, открывают ровно соответствующий набор.
- F3 (ACC-015). Реестр простоев: итог и разбивка из одного набора; открытые — «идёт» с текущей сценарной длительностью.
- F4 (ACC-012). Возврат из детализации сохраняет выбранную причину/фильтры (URL).
- Приёмка: e2e «расшифровка = агрегат» на всех входах.

### PKG-G. Тесты и валидатор — P0-обязательство (ACC-035)

- G1. Новые e2e: ранний возврат заблокирован (ACC-001); финансист/опдиректор не мутирует (ACC-006); SITE_MANAGER в Отчётах видит только свой объект (ACC-007); консистентность доступности робота/объекта между экранами (ACC-023); ролевые главные страницы (ACC-022); живой сценарий даёт 25 мин / 29 167 ₽ и закрывает технедоступность 8 ч 28 мин (ACC-004); сброс восстанавливает контрольные суммы.
- G2. Валидатор: инвариант «нет невозможных состояний после любой последовательности действий» (гейты A1/A2), сценарные метки A4.
- G3. Ручной прогон трёх ролевых сценариев (Отчёт §12) + полный сценарий v1.1 шаги 1–17.
- Приёмка: `npm test` 0 ошибок; e2e зелёные; ручной чек-лист без блокеров.

### PKG-H. Документация и замыкание

- H1. AGENTS.md: контрольный контракт §1, источники (Отчёт), актуализация правил.
- H2. Статус-трекер этого плана; финальный прогон гейтов (lint/format/type-check/build/test/e2e).
- H3. Остаточные P1 (ACC-024 масштаб 72 робота, ACC-033/034 паспорта метрик каталогом, ACC-017/018/021 недоделки карточек) — задокументированы как следующий пакет с оценкой; не блокируют повторную приёмку P0.

# 3. Порядок и гейты

Последовательность Отчёта §7: контракт (сделано документально) → домен/гейты (PKG-A) → права (PKG-B) → метрики (PKG-C) → ролевые страницы (PKG-D) → UX/аналитика (PKG-E/F) → тесты (PKG-G, непрерывно) → документация (PKG-H).

Каждый пакет: код → `npm run lint` / `npm run format:check` / `npm run type-check` / `npm test` чистые → затронутые e2e зелёные → статус в трекере с доказательствами. Право ставить passing — только сквозной e2e-прогон (AGENTS.md «Verification Levels»).

# 4. Трекер статусов

| Пакет           | Статус | Доказательства                                                                                                                                                                                        |
| --------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Контракт данных | ✅     | Настоящий файл §1; валидатор 27/27 (`npm test`), 986 667 ₽ / 5 столкновений / 32+1                                                                                                                    |
| PKG-A           | ✅     | `useDemoData.ts`: гейт `returnGate` + сценарные метки `SCENARIO_STEPS_MIN` + permission-гарды на всех мутациях; `OverviewPage.vue` без `Math.random`; e2e ACC-001/004                                 |
| PKG-B           | ✅     | `ReportsPage.vue` под tenant-scope; deep-link guard в `router/index.ts`; SERVICE_ENGINEER → «мои работы»; «Сервис и ТОиР» в сайдбаре; e2e ACC-006/007                                                 |
| PKG-C           | ✅     | `src/data/metrics.ts` (720 робот-ч/робот, 6 240 парк); подключены store/Analytics/Robots/RobotDetails/Sites/SiteDetails/Reports; e2e ACC-023 (99,21 / 99,71 / список=карточка)                        |
| PKG-D           | ✅     | `PortfolioPage.vue` + `FinanceHomePage.vue` + ролевые лендинги; e2e ACC-022                                                                                                                           |
| PKG-E           | ✅     | События → INC-2026-XXXX (e2e ACC-008); «интервал» → термины простоев (23 замены); Ronavi/HIK/Geek+; FMR-001 паддинг; «90-й перцентиль»; кнопка безопасности убрана (авто при назначении координатора) |
| PKG-F           | ✅     | `causeRows` считает сущности из строк детализации (ACC-011); карточки потерь → простои объекта (ACC-013); сводка «подтверждено N: влияние X · недоступность Y» + «идёт …» (ACC-015)                   |
| PKG-G           | ✅     | `e2e/acc-fixes.spec.ts` — 7 тестов (ACC-001/004/006/007/022/023/008); адаптация demo-b/d/e; итог: 23/23 e2e passed                                                                                    |
| PKG-H           | ✅     | AGENTS.md дополнен (контракт, источники); полный прогон: lint/format/type-check/build/test(27)/e2e(23) — чистые                                                                                       |

Итог итерации: все P0-замечания Отчёта (ACC-001…008, 022, 023, 027, 029, 032) закрыты кодом и покрыты e2e. Остаток на следующий пакет: ACC-024 (масштаб 72 робота — пересборка сумм), ACC-033/034 (каталог паспортов метрик), ACC-012 (полное сохранение контекста детализации), ACC-017/018/021, G2-инвариант валидатора на последовательности действий, G3 ручной прогон трёх ролевых сценариев по Отчёту §12.
